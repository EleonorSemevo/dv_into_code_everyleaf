fr:
  activerecord:
    models:
      task: tache
    attributes:
      task:
        name: nom
        content: contenu
        status: statut
        priority: priorite
        limit_date: date limite
